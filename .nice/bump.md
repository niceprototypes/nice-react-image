[2026-05-24 06:00] major: Drop the no-op `ImageStyles` ComponentType export. Token CSS is provided by build-time `nice-styles/tokens.css` (loaded by `StylesProvider`); `ImageStyles` rendered nothing and is removed from `src/index.ts`, `src/tokens/index.ts`, and `src/tokens/ImageStyles.ts` (file deleted). Consumers using `<StylesProvider componentStyles={[ImageStyles, …]}>` should drop the prop entirely (also removed in nice-react-styles@major).
[2026-05-23 20:07] patch: Drop unused `rollup-plugin-peer-deps-external` devDependency — externalization is now handled by `nice-configuration/rollup`'s built-in peerDependencies reader. No build-output change.
[2026-05-20 15:08] major: `mode` prop now wraps Image in `<Mode name={mode}>` instead of pinning the styled-component to that mode in isolation. StyledImg/StyledBackgroundImage drop `$mode` threading; borderRadius/borderColor/backgroundSize tokens resolve via cascade.
[2026-05-20 13:14] minor: Add bordered, borderWidth, and borderColor props. When bordered is true, the image renders a border using design tokens — borderWidth (default "base") + borderColor (default "base"). Works for both as="img" and as="div" rendering modes. New ImageBorderedType / ImageBorderWidthType / ImageBorderColorType exports.
[2026-05-19 12:00] patch: Update consumer call sites to the unified getToken from nice-react-styles (was getReactToken). Internal-only rename — Image's public props are unchanged. Comments updated to reference nice-styles' renamed CSS artifact (tokens.css).
